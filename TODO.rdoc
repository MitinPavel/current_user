= TODO

* Authentication helpers should be visible in views.
* Add an ability to use an arbitrary class instead of User.
* Create some kind of "convinient" logout url, which effectivelly will take users to the sign_in page
* Create rake tasks to recreate the auth key and to display how the signin url looks like
* Uniform "signin" and "sign in"
* Add I18n.
